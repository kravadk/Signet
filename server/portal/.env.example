# WalrusForge portal — copy to .env (or set in your host dashboard).
PORT=8790
# The public origin used in canonical + og:url links (no trailing slash).
PUBLIC_ORIGIN=https://walrusforge.app
# Network ids are read from move/walrusforge/deployments.json (testnet + mainnet).
