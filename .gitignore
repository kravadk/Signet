# Dependencies
node_modules/
**/node_modules/

# Build output
**/build/
**/.next/
**/dist/
**/out/
*.tsbuildinfo
coverage/
playwright-report/
test-results/

# Move build
move/signet/build/

# Indexer database (rebuildable from chain)
server/*.db
server/*.db-*

# Local CLI state + demo scratch
**/.forge/
demo/sample-repo/

# Secrets — never commit keys
*.key
.env
.env.*
*.agent.env
!.env.example

# Transient CLI / deploy output artifacts (not source)
move/signet/*-out.json
move/signet/*-err.txt
move/signet/mn-*.json
move/signet/publish*.json
move/signet/forkreg*.json
move/signet/privreg*.json
move/signet/upgrade-*
.claude/scheduled_tasks.lock

# Scratch / backups (not part of the shipped project)
web-next-backup/
web-annotate/

# OS / editor
.DS_Store
*.log
